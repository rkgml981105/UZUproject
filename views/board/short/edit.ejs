<!DOCTYPE html>
<html>
  <head>
    <%- include('../../partials/head') %>
  </head>
  <body>
    <%- include ("../../partials/nav") %>
    <div class="container">
      <div class = "btn-container back">
        <a href="/board/short"><i class="material-icons back">arrow_back</i></a>
      </div>
      <div class="input-container">
        <form  class = "spinput" action="/board/short/<%= boards._id %>?_method=put" method="post">
          <div class="sptitleinput">
            <label for="title">제목</label>
            <input type="text" id="title" name="title" value="<%= boards.title %>" autocomplete="off">
          </div>
          <div class="spwriterinput">
            <label for="writer">글쓴이</label>
            <span class="spwriter-show"><%= boards.writer.nickname %></span>
          </div>
          <label for="content">내용</label>
          <div class="spcontentinput input-field">
            <textarea class="materialize-textarea" id="content" name="content"><%= boards.content %></textarea>
          </div>
          <div class = "btn-container submit">
            <button class="btn default" type="submit">게시</button>
          </div>
        </form>
       </div>
    </div>
    <script>
        M.textareaAutoResize($('#content'));
    </script>
  </body>
  <%- include ("../../partials/footer") %>
</html>