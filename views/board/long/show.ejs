<!DOCTYPE html>
<html>
  <head>
    <%- include('../../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="Board_longs-show">
      <h2>Show</h2>
      <div>
        <h3><%= Board_longs.title%></h3>
        <label>writer</label><span><%= Board_longs.writer %></span>
        <br>
        <label>content</label><span><%= Board_longs.content %></span>
      </div>
      <div class="Board_longs-menu">
        <a href="/board/long/<%= Board_longs._id %>/edit">Edit</a> <!-- 1 -->
        <form action="/board/long/<%= Board_longs._id %>?_method=delete" method="post"> <!-- 2 -->
          <a href="#" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>
        </form>
      </div>
    </div>
  </body>
</html>

