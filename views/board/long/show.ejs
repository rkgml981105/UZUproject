<!DOCTYPE html>
<html>
  <head>
    <%- include('../../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="Board_long-show">
      <h2>Show</h2>
      <div>
        <h3><%= LongBoard.title%></h3>
        <label>writer</label><span><%= LongBoard.writer %></span>
        <br>
        <label>content</label><span><%= LongBoard.content %></span>
      </div>
      <div class="Board_long-menu">
        <a href="/board/long/<%= LongBoard._id %>/edit">Edit</a> <!-- 1 -->
        <form action="/board/long/<%= LongBoard._id %>?_method=delete" method="post"> <!-- 2 -->
          <a href="#" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>
        </form>
      </div>
    </div>
  </body>
</html>

