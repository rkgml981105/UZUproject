<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ("./partials/head") %>
    <script>
        $(document).ready(function() {
            var browserwidth = $(window).width();
            var browserheight = $(window).height();
            
            const resizeBg = () => {
                $('.bg').width(browserwidth);
                $('.bg').height(browserheight);
            }

            resizeBg();
        });
    </script>
    <link rel="stylesheet" href="/css/login.css">
</head>
<body>
    <div class="bg"></div>
    <div class="m-bg"><img src = "/image/uzu_bg.jpg"></div>
    <div class="main-logo">
        <img src="image/brand_logo.png">
    </div>
    <div class="loginCon z-depth-3">
        <% if (password == false) { %>
            <script>alert('아이디 혹은 비밀번호가 올바르지 않습니다')</script>
        <% } %> 
        <form action="/member/login" method="POST" id="loginform">
            <div>
                <input id="id-email" type="email" name="email">
                <label for="id-email">아이디/이메일</label>
            </div>
            <div>
                <input id="password" type="password" name="password">
                <label for="password">비밀번호</label>
            </div>
            <button class="loginbtn waves-light btn" type="submit">로그인</button>
        </form>     
        <div class="tosignin">회원이 아니신가요? <a href="/register">회원가입</a></div>
        <div class="or">혹은</div>
        <button class="g loginbtn waves-light btn" type="submit">Google로 로그인</button>
    </div>
</body>
</html>